
<div class="col-lg-4 col-md-6 col-sm-12">
  <div class="card">
    <div class="card-body">
      <h4 class="card-title mb-2">{{title}}</h4>
      <img src="{{imageOneUrl}}" class="img-fluid"/>
      <a href="/posts/{{id}}" class="btn btn-primary">View Full Post</a>
      <div>
        <span>{{numberAgree}} Agree</span><span>{{numberDisagree}} Disagree</span>
      </div>
    </div>
  </div>
</div>
